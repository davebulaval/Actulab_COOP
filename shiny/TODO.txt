Correction bug break point
Erreur division quartile
if ( length(unique(prediction)) == 0) then pas de couleur
else ( normal)

-ajout de toutes les âges dans âges

-ajout variable sexe unique avec embranchement pour ne pas faire effet double

coder les personas - menu déroulant permettant d'afficher les personnas